"""
MCP Fetch Server Module Entry Point

Enables running the fetch server as a Python module using:
    python -m mcp_server_fetch

This file serves as the entry point when the package is executed
as a module, delegating to the main function defined in __init__.py
"""

from mcp_server_fetch import main

main()
